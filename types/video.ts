export interface VideoInfo {
    videoId: string;
    title: string;
    channelName: string;
    publishedAt: string;
    thumbnailUrl: string;
}

export interface VideoMetadata {
    url: string;
    videoId: string;
} 